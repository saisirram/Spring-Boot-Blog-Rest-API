
# DataSource Properties
spring.datasource.url = jdbc:mysql://localhost:3306/myblog?useSSL=false&serverTimezone=UTC
spring.datasource.username = root
spring.datasource.password = root

#Hibernate Properties
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

#ddl-auto means when ever we restarts the system if the database not exists it creates one or else updates the existing one.
spring.jpa.hibernate.ddl-auto = update 

logging.level.org.springframework.security = DEBUG

#spring.security.user.name = saisriram
#spring.security.user.password = saiv31
#spring.security.user.roles = ADMIN